# Next Task: Document Management UI

Implement document upload and replacement in the compliance app. Preserve the previous version by setting `is_current_version=False`, validate expiry requirements from `DocumentType`, and store files under the configured documents directory. Add focused tests before changing the dashboard.
